import T from 'prop-types'

export const ProposalPropTypes = T.shape({
  accepted: T.string,
  allVotes: T.string,
  address: T.string,
  checkedTimes: T.number,
  code: T.string,
  codeName: T.string,
  compareErrors: T.string,
  description: T.string,
  emergency: T.bool,
  endBlock: T.number,
  hardCapReached: T.bool,
  key: T.string,
  lastCheckedBlock: T.number,
  leading: T.bool,
  location: T.string,
  myAccepts: T.string,
  myRefuses: T.string,
  myVotes: T.string,
  refused: T.string,
  replaces: T.string,
  replacesCode: T.string,
  result: T.bool,
  resultBlock: T.number,
  surveyEnd: T.bool,
  terminated: T.bool,
  terminationData: T.object,
  withdrawed: T.bool,
}).isRequired
